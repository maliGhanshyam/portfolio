services:
  - type: static
    name: portfolio
    plan: free
    branch: main
    installCommand: npm install
    buildCommand: npm run build
    publishPath: build
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
